<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zx.myownbaseapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <!--启动页-->
        <activity android:name=".mvp.view.activity.LunchActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--第一次的欢迎页-->
        <activity android:name=".mvp.view.activity.WelcomeActivity" android:launchMode="singleTop"/>
        <!--主页-->
        <activity android:name=".mvp.view.activity.MainActivity" android:launchMode="singleTop" />
    </application>

</manifest>